
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pipeline-scraper"
version = "1.0"
description = "Pipeline scraper for Halozyme's partners"
requires-python = ">=3.10"
